# C/C++ Interface Segregation (Client-Library Architecture)

## Overview
Demonstrates software architecture principles by isolating core logic into a reusable library (`Lib2`) separate from the consuming application (`Client`). 

## Technical Scope
- Validates header isolation, binary linkage, and modular compilation constraints.
- Addresses symbol resolution and cross-module dependencies in unmanaged environments.